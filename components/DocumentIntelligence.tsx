"use client";
import { useEffect, useState } from "react";
type Finding={item:string;value:string|null;reference_range:string|null;status:"normal"|"abnormal"|"unclear"|"not_reported";evidence:string};
type Analysis={document_type?:string;report_date?:string|null;summary?:string;findings?:Finding[];key_observations?:string[];supportive_findings?:string[];limitations_and_concerns?:string[];possible_associations?:string[];symptom_associations?:string[];red_flags?:string[];questions_for_clinician?:string[];priority?:string;specialty_hint?:string|null;workflow?:string;confidence?:number;requires_human_review?:boolean};
type Document={id:string;original_filename:string;content_type:string|null;file_size_bytes:number|null;ocr_status:string;validation_status:string;processing_status?:string;processing_error?:string|null;created_at:string;metadata?:{ai_analysis?:Analysis}};
function priorityLabel(v?:string){return ({routine:"Routine review",review_soon:"Review soon",urgent_review:"Urgent human review",unclear:"Priority unclear"} as Record<string,string>)[v||""]||"Awaiting review";}
function List({title,items}:{title:string;items?:string[]}){if(!items?.length)return null;return <div className="mt-6"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--muted)]">{title}</p><ul className="mt-3 space-y-2">{items.map((x,i)=><li key={i} className="rounded-2xl bg-white/65 px-4 py-3 text-sm leading-6">{x}</li>)}</ul></div>}
export default function DocumentIntelligence(){const[documents,setDocuments]=useState<Document[]>([]),[file,setFile]=useState<File|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState(""),[success,setSuccess]=useState("");async function load(){const r=await fetch("/api/documents",{cache:"no-store"});if(r.ok)setDocuments((await r.json()).documents||[])}useEffect(()=>{load().catch(()=>undefined)},[]);async function upload(e:React.FormEvent){e.preventDefault();if(!file)return;setBusy(true);setError("");setSuccess("");try{const b=new FormData();b.append("file",file);const r=await fetch("/api/documents",{method:"POST",body:b});const d=await r.json();if(!r.ok)throw new Error(d?.error||"Unable to analyze report");setSuccess(d.workflow?.triggered?"Report analyzed, saved and routed for workflow coordination.":"Report analyzed and saved. Human review is required before clinical action.");setFile(null);const input=document.getElementById("report-file") as HTMLInputElement|null;if(input)input.value="";await load()}catch(e){setError(e instanceof Error?e.message:"Unable to analyze report")}finally{setBusy(false)}}return <div className="space-y-5"><div className="glass rounded-[30px] p-6 md:p-8"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--care)]">AI document intelligence</p><h2 className="mt-1 font-display text-2xl font-extrabold">Upload a patient report.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[var(--muted)]">MediSync extracts evidence, explains documented findings in context, surfaces possible associations and questions for a clinician, and routes the record for human review.</p></div><span className="rounded-full bg-[var(--care-soft)] px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-[var(--care)]">Human-in-the-loop</span></div><form onSubmit={upload} className="mt-7 rounded-[24px] border border-dashed border-black/10 bg-white/65 p-5 md:p-6"><label htmlFor="report-file" className="block text-sm font-bold">Medical report <span className="font-normal text-[var(--muted)]">PDF, PNG, JPEG or WEBP · max 15 MB</span></label><input id="report-file" type="file" accept="application/pdf,image/png,image/jpeg,image/webp" onChange={e=>setFile(e.target.files?.[0]??null)} className="mt-4 block w-full rounded-2xl border border-black/10 bg-white px-4 py-3 text-sm"/>{file&&<p className="mt-2 text-xs text-[var(--muted)]">Selected: {file.name}</p>}{error&&<div className="mt-4 rounded-2xl bg-[var(--danger-soft)] px-4 py-3 text-sm font-semibold text-[var(--danger)]">{error}</div>}{success&&<div className="mt-4 rounded-2xl bg-[var(--care-soft)] px-4 py-3 text-sm font-semibold text-[var(--care)]">{success}</div>}<button disabled={!file||busy} className="mt-5 flex w-full items-center justify-between rounded-2xl bg-[var(--ink)] px-5 py-4 text-sm font-bold text-white shadow-xl disabled:cursor-not-allowed disabled:opacity-45"><span>{busy?"Uploading, processing and analyzing…":"Analyze report"}</span><span>→</span></button></form></div>{documents.length>0&&<div className="space-y-4">{documents.map(doc=>{const a=doc.metadata?.ai_analysis;return <article key={doc.id} className="glass rounded-[30px] p-6 md:p-8"><div className="flex flex-col gap-4 md:flex-row md:items-start md:justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[var(--muted)]">MediSync analyzed record</p><h3 className="mt-1 font-display text-xl font-extrabold">{doc.original_filename}</h3><p className="mt-1 text-xs text-[var(--muted)]">{new Date(doc.created_at).toLocaleString()} · {a?.document_type||doc.content_type||"Document"} · {doc.processing_status||doc.ocr_status}</p></div><span className="rounded-full bg-[var(--warning-soft)] px-3 py-1.5 text-[10px] font-bold uppercase tracking-wider text-[var(--warning)]">{priorityLabel(a?.priority)}</span></div>{a?<><div className="mt-6 rounded-2xl bg-white/70 p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-[var(--muted)]">AI summary</p><p className="mt-2 text-sm leading-6 text-[var(--muted-strong)]">{a.summary}</p></div><div className="mt-5 grid gap-4 md:grid-cols-3"><div className="rounded-2xl bg-white/70 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--muted)]">Specialty hint</p><p className="mt-2 text-sm font-bold">{a.specialty_hint||"General review"}</p></div><div className="rounded-2xl bg-white/70 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--muted)]">Workflow</p><p className="mt-2 text-sm font-bold">{a.workflow||"DOCUMENT"}</p></div><div className="rounded-2xl bg-white/70 p-4"><p className="text-[10px] font-bold uppercase tracking-wider text-[var(--muted)]">Confidence</p><p className="mt-2 text-sm font-bold">{typeof a.confidence==="number"?`${Math.round(a.confidence*100)}%`:"—"}</p></div></div>{a.findings?.length?<div className="mt-6 overflow-hidden rounded-2xl border border-black/5"><div className="bg-black/[.03] px-4 py-3 text-xs font-bold uppercase tracking-wider text-[var(--muted)]">Extracted findings</div><div className="divide-y divide-black/5">{a.findings.map((f,i)=><div key={`${f.item}-${i}`} className="grid gap-2 px-4 py-4 md:grid-cols-[1.1fr_.8fr_.8fr]"><div><p className="text-sm font-bold">{f.item}</p><p className="mt-1 text-xs text-[var(--muted)]">{f.evidence}</p></div><div className="text-sm">{f.value||"Not reported"}</div><div className="text-xs font-semibold uppercase tracking-wide text-[var(--muted)]">{f.status.replaceAll("_"," ")}{f.reference_range?` · ${f.reference_range}`:""}</div></div>)}</div></div>:null}<List title="Positive / supportive findings" items={a.supportive_findings}/><List title="Possible associations — not a diagnosis" items={a.possible_associations}/><List title="Symptoms potentially associated with documented findings" items={a.symptom_associations}/><List title="Limitations / concerns" items={a.limitations_and_concerns}/><List title="Red flags documented in the source" items={a.red_flags}/><List title="Questions to discuss with your clinician" items={a.questions_for_clinician}/><div className="mt-6 rounded-2xl bg-[var(--care-soft)] px-4 py-3 text-xs font-semibold leading-5 text-[var(--muted-strong)]">AI-assisted evidence analysis only. These associations are not a diagnosis or treatment recommendation. Authorized clinician review is required before clinical decisions or action.</div></>:<div className="mt-5 rounded-2xl bg-white/70 p-5"><p className="text-sm font-bold">Processing status: {doc.processing_status||doc.ocr_status}</p><p className="mt-2 text-sm text-[var(--muted)]">{doc.processing_error||"Your document is being processed. The completed structured analysis will appear here."}</p></div>}</article>})}</div>}</div>}
